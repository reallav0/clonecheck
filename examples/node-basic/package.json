{
  "name": "node-basic",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "vite": "^6.0.0"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
